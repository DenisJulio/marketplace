spring:
  datasource:
    username: denis
    password: password
    url: jdbc:postgresql://localhost:5432/db
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
         format_sql: true
logging:
  level:
    org:
      springframework:
        security: info
